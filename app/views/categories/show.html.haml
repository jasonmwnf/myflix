%section.genre
  %header.clearfix
    %h1= @category.name
  %article.row
    - @category.videos.each do |vid|
      .video.col-md-2
        = link_to image_tag(vid.small_cover_url), "/video/#{vid.id}"