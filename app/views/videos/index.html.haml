%article.video_category
  - @categories.each do |cat|
    %header
      %h3= link_to cat.name, cat
      %br
      .videos.row
        - cat.recent_videos.each do |vid|
          .video.col-sm-2
            = link_to image_tag(vid.small_cover_url), vid